# results/

The small, keepable outputs you'll cite in your report:
- `baseline_metrics.json` — the "before" numbers (from script 02).
- `after_<run>.json` — the "after" numbers (from script 04).
- plots you generate comparing methods.
