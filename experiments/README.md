# experiments/

One sub-folder per training run (e.g. `lora_counterfactual_run/`), holding the
checkpoint and training logs. Git-ignored because checkpoints are large and
regenerable. The folder name encodes method + data so runs don't overwrite.
