# NOTE: torch is installed separately in setup.sh with the correct CUDA wheel
# (the default pip torch can run on CPU or mismatch the cluster driver).
# Do not add torch here.

# Hugging Face stack
transformers>=4.44
datasets>=2.20
accelerate>=0.33
peft>=0.12
# NOTE: trl (for the GRPO strategy) is intentionally NOT here. No trl works on the
# main venv's cu121 torch 2.5.1: modern trl needs FSDPModule (torch>=2.6), and the
# old trl that runs on 2.5.1 (0.14/0.15) hard-imports mergekit+vllm. GRPO therefore
# lives in a SEPARATE env, .venv-grpo (torch>=2.6 + latest trl), built by
# setup_grpo.sh — the main stack (Full-FT / LoRA / self-distill / eval / spectral)
# stays trl-free and on its validated cu121 torch 2.5.1.
# 8-bit AdamW + 4-bit (NF4) loading, to fit 7B full-parameter unlearning on 40GB.
bitsandbytes>=0.43
# DeepSpeed ZeRO-3 for the learn phase (fp32 master weights, exactly like the TOFU repo).
deepspeed>=0.14

# Tokenizer backends required by phi-2 (and most modern tokenizers)
tiktoken
sentencepiece

# Config, data, utilities
pyyaml
numpy
pandas
tqdm
scikit-learn
scipy
rouge_score

# Plotting
matplotlib
